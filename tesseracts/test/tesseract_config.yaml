# Tesseract configuration file for GPU deployment

name: "vlasov"
version: "0.1.0"
description: ""

build_config:
  base_image: "python:3.11-bookworm"
  target_platform: "native"
  custom_build_steps:
    - "ENV GIT_PYTHON_REFRESH=quiet"
  # requirements: "tesseract_requirements_gpu.txt"
