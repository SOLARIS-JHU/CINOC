name: "solver_heat2d_decentralized"
version: "0.1.0"
description: "2D Heat Equation Solver with Decentralized Control (ADI Method)"

build_config:
  target_platform: "native"

  package_data:
    - ["solver.py", "solver.py"]
    - ["models/policy.py", "models/policy.py"]
